<rml>
<head>
    <title>Main Menu</title>
    <style>
        body {
            font-family: LatoLatin;
            font-size: 18px;
            color: #ffffff;
            background-color: transparent;
            margin: 0;
            padding: 0;
        }
        
        /* Overlay de fondo oscuro */
        #main-menu-overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.92);
            z-index: 1000;
        }
        
        /* Contenedor principal del menú */
        #main-menu-container {
            position: absolute;
            inset: 7.5% 7.5%;
            width: auto;
            height: auto;
            min-width: 600px;
            min-height: 500px;
            max-width: 1000px;
            max-height: 700px;
            margin: auto;
            background-color: rgba(15, 25, 40, 0.98);
            border: 3px #4a9eff;
            border-radius: 20px;
            padding: 60px 50px;
            text-align: center;
            animation: fadeIn 0.5s ease-in;
            box-sizing: border-box;
        }
        
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: scale(0.9);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }
        
        /* Título principal */
        #game-title {
            color: #4a9eff;
            font-size: 4.5em;
            font-weight: bold;
            margin-bottom: 30px;
            font-effect: glow(2px 20px 0px 0px rgba(74, 158, 255, 0.8));
            letter-spacing: 4px;
            animation: glow 2s ease-in-out infinite alternate;
        }
        
        @keyframes glow {
            from {
                font-effect: glow(2px 20px 0px 0px rgba(74, 158, 255, 0.8));
            }
            to {
                font-effect: glow(3px 40px 0px 0px rgba(74, 158, 255, 1));
            }
        }
        
        /* Subtítulo */
        #game-subtitle {
            color: #aaccff;
            font-size: 1.5em;
            margin-bottom: 60px;
            opacity: 0.9;
        }
        
        /* Botón de empezar */
        #start-button {
            display: block;
            width: 400px;
            max-width: 80%;
            margin: 40px auto;
            padding: 25px 50px;
            background-color: #4a9eff;
            color: #ffffff;
            font-size: 2em;
            font-weight: bold;
            border: 3px #6ab0ff;
            border-radius: 15px;
            cursor: pointer;
            text-align: center;
            text-decoration: none;
            letter-spacing: 3px;
        }
        
        #start-button:hover {
            background-color: #5ab0ff;
            border-color: #8ac0ff;
        }
        
        #start-button:active {
            background-color: #3a8fef;
        }
        
        /* Información adicional */
        #menu-info {
            margin-top: 50px;
            padding-top: 30px;
            border-top: 1px rgba(74, 158, 255, 0.3);
            color: #8899aa;
            font-size: 1em;
        }
        
        #menu-info p {
            margin: 10px 0;
        }
    </style>
</head>
<body>
    <div id="main-menu-overlay">
        <div id="main-menu-container">
            <h1 id="game-title">SHOOTER</h1>
            <p id="game-subtitle">Bienvenido al juego</p>
            
            <button id="start-button">EMPEZAR</button>
            
            <div id="menu-info">
                <p>Presiona F8 para abrir el debugger de RmlUi</p>
                <p>Usa WASD para moverte, ESPACIO para saltar</p>
            </div>
        </div>
    </div>
</body>
</rml>
